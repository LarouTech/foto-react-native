import { StyleSheet, Text} from 'react-native';

interface {{title}}Props { }

function {{title}}(props: {{title}}Props) {
    return <Text>{{title}}</Text>
}

const styles = StyleSheet.create({})

export default {{title}}